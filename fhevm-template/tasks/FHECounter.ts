import { task } from "hardhat/config";

task("fhe-counter", "Placeholder FHE Counter task")
    .setAction(async (_taskArgs, _hre) => {
        console.log("FHE Counter task placeholder");
    });
